90*4
5280/360
c
pg.out.m2
tblkeep
dim(geno)
  geno <- geno[!duplicated(paste(geno$ped, geno$person, sep="-")),]
    keepped <- match(paste(geno$ped, geno$person, sep="-"),
                     paste(ped$ped, ped$person, sep="-"))
table(keepped)
di(geno)
dim(geno)
c
pg.out
tblkeep
dim(geno)
geno[1:5,1:5]
  geno <- geno[!(paste(geno$ped, geno$person, sep="-") %in% paste(ped$ped, ped$person, sep="-")),]
    keepped <- match(paste(geno$ped, geno$person, sep="-"),
                     paste(ped$ped, ped$person, sep="-"))
geno[1:5,1:5]
Q
geno[1:5,1:5]
 geno <- geno[(paste(geno$ped, geno$person, sep="-") %in% paste(ped$ped, ped$person, sep="-")),]
    keepped <- match(paste(geno$ped, geno$person, sep="-"),
                     paste(ped$ped, ped$person, sep="-"))
geno[1:5,1:5]
keepped
Q
pg.out
options()$warn
options()$warning
options()$warnings
traceback()
table(keepped)
dim(geno)
geno[1:5,1:5]
keepped
c
dim(geno)
geno[1:5,1:5]
table(keepped)
any(is.na(tblkeep))
any(is.na(names(tblkeep)))
if(any(is.na(keepped)) { 
    warning(paste("subject in genotype matrix but not in pedigree; removing: ", names(tblkeep)[which(is.na(tblkeep))], ".\n", sep=""))
    geno <- geno[!is.na(keepped),]
    keepped <- match(paste(geno$ped, geno$person, sep="-"),
                     paste(ped$ped, ped$person, sep="-"))
  }
  if(any(is.na(keepped))) { 
    warning(paste("subject in genotype matrix but not in pedigree; removing: ", names(tblkeep)[which(is.na(tblkeep))], ".\n", sep=""))
    geno <- geno[!is.na(keepped),]
    keepped <- match(paste(geno$ped, geno$person, sep="-"),
                     paste(ped$ped, ped$person, sep="-"))
  }
dim(geno)
keepped
c
warnings()
warnings()
 for(f in rfiles) source(paste("../R/",f,sep=""))
keepped
c
  for(f in rfiles) source(paste("../R/",f,sep=""))
install.packages("CompQuadForm", lib="/projects/bsi/gentools/R/lib311")
q()
n
dim(example.geno)
names(example.geno)
class(example.geno)
class(map)
class(example.map)
q()
n
